package com.geom4rios.javaproducerconsumer.consumer;

import static org.junit.jupiter.api.Assertions.*;

class ConsumerTest {

    // test io consumer will consume only io tasks

    // test cpu consumer will consume only cpu tasks

    // test memory consumer will consume only memory tasks
}